// Site configuration. Change PASSWORD to your desired password.
// WARNING: This exposes the password to anyone who can fetch the site files.
// For stricter security, manage the password server-side and validate via API.
window.SITE_CONFIG = {
  PASSWORD_ENABLED: true,
  PASSWORD: "changeme" // change this
};
